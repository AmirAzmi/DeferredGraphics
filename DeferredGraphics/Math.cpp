#include "Math.h"

bool Math::PointOnLine(const glm::vec3& point, const Line& line)
{
  return false;
}

bool Math::PointPlane(const glm::vec3& point, const Plane& plane)
{
  return false;
}

bool Math::LinePlane(const Line& point, const Plane& plane)
{
  return false;
}

bool Math::PlanePlane(const Plane& plane, const Plane& planetwo)
{
  return false;
}
